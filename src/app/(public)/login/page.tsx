import { LoginForm } from "@/features/auth/components/login-form";

export default function Page() {
  return (
    <main className="mx-auto max-w-md h-full p-6 flex items-center justify-center">
      <LoginForm />
    </main>
  );
}
